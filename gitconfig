[alias]
  alias = !sh -c 'git config --get alias.$1' -
  b = branch
  ci = commit
  co = checkout
  com = checkout master
  cp = cherry-pick
  fixup = !sh -c 'git rebase -i --autosquash head~$1' -
  l = log --date=relative
  ll = !git l --stat
  rb = !grb
  repush = !git pull --rebase && git push
  st = status -sb
  staged = diff --cached
  stashed = "!for NAME in $(git stash list | cut -d : -f 1); do echo \"$NAME\"; git show \"$NAME\"; done"
  su = submodule update
  unstage = reset HEAD --
  web = instaweb --httpd=webrick
  who = shortlog -s --

[color]
  diff = auto
  status = auto
  branch = auto

[core]
  pager = less -rciM
  excludesfile = /Users/rick/.gitignore

[github]
  user = rfletcher
  token = 1178dbb953eee29648eb032c8fcb1c79

[user]
  email = fletch@pobox.com
  name = Rick Fletcher

[merge]
  tool = opendiff

[push]
  default = tracking
